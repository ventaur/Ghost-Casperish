{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

{{#if @custom.tease_inaccessible_content}}
    {{#get "posts" limit=@config.posts_per_page include="tags"}}
        {{> "listing"}}
    {{/get}}
{{else if @member.paid}}
    {{#get "posts" limit=@config.posts_per_page filter="visibility:[paid, members, public]" include="tags"}}
        {{> "listing"}}
    {{/get}}
{{else if @member}}
    {{#get "posts" limit=@config.posts_per_page filter="visibility:[members, public]" include="tags"}}
        {{> "listing"}}
    {{/get}}
{{else}}
    {{#get "posts" limit=@config.posts_per_page filter="visibility:public" include="tags"}}
        {{> "listing"}}
    {{/get}}
{{/if}}